<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Активное состояние (включен) -->
    <item android:color="@color/switch_track_color_light" android:state_checked="true" />
    <!-- Неактивное состояние (выключен) -->
    <item android:color="@color/switch_track_color_dark" android:state_checked="false" />
</selector>